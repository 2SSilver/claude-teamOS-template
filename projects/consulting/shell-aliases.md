# Shell Aliases — Example Project

> **TODO:** Replace with project-specific shell aliases. See the root `shell-aliases.md` for pattern.

Add shortcuts for common tasks in this project. Update your shell config and run `source ~/.zshrc`.

```bash
# Example: launch Claude in this project with /prime
alias ep='cd /path/to/projects/example-project && claude "/prime"'
```
