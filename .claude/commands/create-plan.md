Invoke the `writing-plans` skill from the `superpowers` plugin to plan the following request:

$ARGUMENTS

Plans for this workspace are saved to `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md` — the superpowers default location. No location override is needed.
